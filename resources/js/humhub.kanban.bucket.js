/* global humhub: false */
humhub.module('kanban.Bucket', function (module, require, $) {
    var Widget = require('ui.widget').Widget;

    var Bucket = Widget.extend();

    Bucket.prototype.init = function () {

    };

    module.export = Bucket;
});
